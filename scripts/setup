#/bin/bash

# Description: Copy scripts to ~/.local/bin


echo "Copying scripts to ~/.local/bin..."

for script in *; do
    [[ -x "$script" ]]
    cp "$script" ~/.local/bin/"$script"
done

echo "Scripts copied!"
