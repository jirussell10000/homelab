services:
  ollama:
    container_name: ollama-home
    image: ollama/ollama:0.33.2
    ports:
      - "11434:11434"
    volumes:
      - ~/ollama:/root/.ollama
    restart: unless-stopped

volumes:
  ollama:
